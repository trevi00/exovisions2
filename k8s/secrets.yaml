# IMPORTANT: This is a template. Do NOT commit actual secrets to Git.
# Use Sealed Secrets, External Secrets Operator, or Vault in production.
#
# To create base64 encoded secrets:
# echo -n "your-secret-value" | base64
#
apiVersion: v1
kind: Secret
metadata:
  name: exoplanet-secrets
  namespace: exoplanet
type: Opaque
data:
  # Database credentials (base64 encoded)
  # Replace these with actual base64 encoded values
  POSTGRES_USER: ZXhvcGxhbmV0        # exoplanet
  POSTGRES_PASSWORD: Y2hhbmdlX21lX2luX3Byb2R1Y3Rpb24=  # change_me_in_production

  # DATABASE_URL will be constructed in deployment from above values
